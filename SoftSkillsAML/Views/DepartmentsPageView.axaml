<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="using:SoftSkillsAML.Converters"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="600"
             x:Class="SoftSkillsAML.DepartmentsPageView"
             xmlns:vm="using:SoftSkillsAML.ViewModels"
             x:DataType="vm:DepartmentsPageViewModel">
  <UserControl.Resources>
    <conv:BytesToImageConverter x:Key="BytesToImageConverter"/>
  </UserControl.Resources>
  <Grid Margin="16" RowDefinitions="Auto,Auto,*">
    <TextBlock FontSize="24" FontWeight="Bold" Text="Список направлений" Margin="0,0,0,12"/>
    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
      <Button Content="Открыть задания" Command="{Binding OpenDepartment}"/>
      <Button Content="Личный кабинет" Command="{Binding OpenProfile}"/>
      <Button Content="Выйти" Command="{Binding Logout}"/>
    </StackPanel>
    <ListBox Grid.Row="2" ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}">
      <ListBox.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Rows="2"/>
        </ItemsPanelTemplate>
      </ListBox.ItemsPanel>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Border BorderBrush="#DDD" BorderThickness="1" Padding="8" Margin="6">
            <StackPanel Width="220" Spacing="8">
              <Image Height="120" Stretch="UniformToFill" Source="{Binding Image, Converter={StaticResource BytesToImageConverter}}"/>
              <TextBlock Text="{Binding Name}" FontWeight="Bold" TextWrapping="Wrap"/>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
